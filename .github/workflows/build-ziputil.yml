name: Build ZipUtil JAR (Java 7)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 7
        uses: actions/setup-java@v3
        with:
          java-version: 7
          distribution: temurin

      - name: Compile Java source (Java 7)
        run: |
          mkdir -p bin
          javac -source 1.7 -target 1.7 -d bin ZipUtil.java

      - name: Create executable JAR
        run: |
          cd bin
          jar cfe ../ZipUtil.jar ZipUtil ZipUtil.class

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v3
        with:
          name: ZipUtil
          path: ZipUtil.jar
